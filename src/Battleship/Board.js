/**
 * A ten-by-ten grid containing a player's shots.
 */
class Board {
  constructor() {
    this.grid = new Array(10).fill(new Array(10).fill(0));
  }

  placeShips(carrier, battleship, cruiser, submarine, destroyer) {

  }

}

export default Board;
